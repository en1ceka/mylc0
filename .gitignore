__pycache__/
*.pyc
checkpoints/
networks/*
# the tuner and the engine need one real network in a fresh
# clone; per-generation snapshots stay local
!networks/latest.mylc0
data/
runs/
stats/
smoke/
*.onnx

# R2 credentials: never committed.
.env
